diff --git a/app/src/main/cpp/hexagon/ivanna_dsp.cpp b/app/src/main/cpp/hexagon/ivanna_dsp.cpp
--- a/app/src/main/cpp/hexagon/ivanna_dsp.cpp
+++ b/app/src/main/cpp/hexagon/ivanna_dsp.cpp
@@
 //  ivanna_dsp.cpp — Runtime FastRPC loader (dlopen/dlsym) + CPU fallback bridge
 //  © 2026 Luis Uriel Pimentel Pérez — GORE TNS. All rights reserved.
 //
-//  Fase H: implementación real del "stub" generado por qaic.
+//  Fase H+ : loader corregido — separa TRANSPORTE (libcdsprpc/libadsprpc)
+//            del STUB IDL (libivanna_dsp_stub.so). Añade selección de dominio
+//            cDSP vía remote_session_control y permite recarga tras release().
 //
 //  Comportamiento:
-//    1. Intenta cargar dinámicamente libcdsprpc.so (cDSP) y como respaldo
-//       libadsprpc.so (aDSP) usando dlopen(..., RTLD_NOW | RTLD_LOCAL).
-//    2. Resuelve las 5 funciones IDL exportadas por el stub qaic real:
-//         ivanna_dsp_open, ivanna_dsp_close, ivanna_dsp_process_stereo,
-//         ivanna_dsp_set_neuro_params, ivanna_dsp_get_metrics
+//    1. Carga el TRANSPORTE FastRPC (libcdsprpc.so → libadsprpc.so).
+//       Resuelve remote_handle64_{open,invoke,close}, remote_session_control,
+//       rpcmem_{init,deinit,alloc,free}. Sin esto el DSP es inalcanzable.
+//    2. Fija el dominio activo (CDSP=3, preferido; ADSP=0 respaldo) con
+//       remote_session_control(FASTRPC_SET_DOMAIN, ...).
+//    3. Carga el STUB IDL local (libivanna_dsp_stub.so) generado por qaic,
+//       que exporta ivanna_dsp_open/close/process_stereo/set_neuro_params/
+//       get_metrics y enruta al skel del DSP a través del transporte.
+//    4. Si el stub aún no existe (fase de bring-up), degrada limpio: reporta
+//       DSP no disponible y el pipeline conmuta a CPU (FIRUpsamplerEngine).
 //    3. Si cualquier símbolo no está presente marca g_dsp_available=false
 //       y todas las funciones retornan -ENOSYS de forma segura para que el
 //       llamador conmute a la ruta CPU (FIRUpsamplerEngine).
-//    4. Es thread-safe: la carga se hace una única vez con std::call_once.
+//    5. Thread-safe: carga protegida por mutex re-entrante (permite recargar
+//       tras release(), a diferencia del std::call_once anterior).
 //    5. Nunca lanza excepciones; nunca aborta si el DSP no está disponible.
 //
@@
 #include "ivanna_dsp.hpp"
 
 #include <atomic>
 #include <cstdint>
 #include <cstring>
 #include <mutex>
 
 #if defined(__ANDROID__) || defined(__linux__)
   #include <dlfcn.h>
   #define IVANNA_HAS_DLOPEN 1
 #else
   #define IVANNA_HAS_DLOPEN 0
 #endif
 
 #ifdef __ANDROID__
   #include <android/log.h>
   #define IVLOG_TAG "IvannaDSP"
   #define IVLOGI(...) __android_log_print(ANDROID_LOG_INFO,  IVLOG_TAG, __VA_ARGS__)
   #define IVLOGW(...) __android_log_print(ANDROID_LOG_WARN,  IVLOG_TAG, __VA_ARGS__)
   #define IVLOGE(...) __android_log_print(ANDROID_LOG_ERROR, IVLOG_TAG, __VA_ARGS__)
 #else
   #define IVLOGI(...) do {} while (0)
   #define IVLOGW(...) do {} while (0)
   #define IVLOGE(...) do {} while (0)
 #endif
 
+// FastRPC transport constants (evita depender del Hexagon SDK en el árbol).
+// Fuente: AOSP `frameworks/av` y Qualcomm HIDL fastrpc — valores estables.
+#ifndef FASTRPC_ADSP_DOMAIN
+#define FASTRPC_ADSP_DOMAIN   0
+#endif
+#ifndef FASTRPC_MDSP_DOMAIN
+#define FASTRPC_MDSP_DOMAIN   1
+#endif
+#ifndef FASTRPC_SDSP_DOMAIN
+#define FASTRPC_SDSP_DOMAIN   2
+#endif
+#ifndef FASTRPC_CDSP_DOMAIN
+#define FASTRPC_CDSP_DOMAIN   3
+#endif
+// remote_session_control request codes
+#ifndef FASTRPC_THREAD_PARAMS
+#define FASTRPC_THREAD_PARAMS 1
+#endif
+#ifndef DSPRPC_SET_DOMAIN
+#define DSPRPC_SET_DOMAIN     7
+#endif
+
 namespace ivanna { namespace hexagon { namespace rt {
 
 // ── Firmas IDL (equivalentes a las declaradas en ivanna_dsp.h) ───────────────
 using ivanna_dsp_handle_t = void*;
 
 using fn_open_t              = int (*)(ivanna_dsp_handle_t*);
 using fn_close_t             = int (*)(ivanna_dsp_handle_t);
 using fn_process_stereo_t    = int (*)(ivanna_dsp_handle_t,
                                        const float*, const float*,
                                        float*, float*, int);
 using fn_set_neuro_params_t  = int (*)(ivanna_dsp_handle_t,
                                        float, float, float, float);
 using fn_get_metrics_t       = int (*)(ivanna_dsp_handle_t, float*, float*);
 
+// ── Firmas del TRANSPORTE FastRPC (libcdsprpc / libadsprpc) ──────────────────
+using fn_remote_session_control_t =
+    int (*)(uint32_t req, void* data, uint32_t datalen);
+using fn_rpcmem_init_t    = void (*)(void);
+using fn_rpcmem_deinit_t  = void (*)(void);
+
 // ── Estado global del loader (opaco al resto del código) ─────────────────────
 namespace {
 
 struct DspVTable {
     fn_open_t             open              = nullptr;
     fn_close_t            close             = nullptr;
     fn_process_stereo_t   process_stereo    = nullptr;
     fn_set_neuro_params_t set_neuro_params  = nullptr;
     fn_get_metrics_t      get_metrics       = nullptr;
 };
 
-std::once_flag           g_load_once;
+struct TransportVTable {
+    fn_remote_session_control_t session_control = nullptr;
+    fn_rpcmem_init_t            rpcmem_init     = nullptr;
+    fn_rpcmem_deinit_t          rpcmem_deinit   = nullptr;
+};
+
+std::mutex               g_load_mtx;
 std::atomic<bool>        g_dsp_available{false};
 DspVTable                g_vt{};
-void*                    g_lib_handle = nullptr;   // dlopen handle (cDSP o aDSP)
-const char*              g_lib_loaded = nullptr;   // nombre de la librería activa
+TransportVTable          g_tv{};
+void*                    g_transport_handle = nullptr; // libcdsprpc / libadsprpc
+void*                    g_stub_handle      = nullptr; // libivanna_dsp_stub.so
+const char*              g_transport_name   = nullptr;
+int                      g_active_domain    = -1;
 
-// Orden de búsqueda: cDSP primero (baja latencia, Q66xx/SM8xxx),
-// luego aDSP como respaldo (chips más antiguos).
-constexpr const char* kFastRpcLibs[] = {
-    "libcdsprpc.so",
-    "libadsprpc.so",
+// Transport: cDSP primero (baja latencia, SM8xxx), aDSP respaldo.
+struct TransportCandidate { const char* lib; int domain; };
+constexpr TransportCandidate kTransports[] = {
+    { "libcdsprpc.so", FASTRPC_CDSP_DOMAIN },
+    { "libadsprpc.so", FASTRPC_ADSP_DOMAIN },
 };
+// Stub IDL local (generado por qaic desde ivanna_dsp.idl).
+constexpr const char* kStubLib = "libivanna_dsp_stub.so";
 
 template <typename FnPtr>
 static FnPtr resolve(void* lib, const char* sym) {
 #if IVANNA_HAS_DLOPEN
     dlerror(); // limpia error previo
     void* p = dlsym(lib, sym);
     const char* err = dlerror();
     if (err != nullptr || p == nullptr) {
         IVLOGW("dlsym('%s') fallo: %s", sym, err ? err : "nullptr");
         return nullptr;
     }
     return reinterpret_cast<FnPtr>(p);
 #else
     (void)lib; (void)sym; return nullptr;
 #endif
 }
 
-static void load_once() {
+static bool try_load_locked() {
 #if IVANNA_HAS_DLOPEN
-    for (const char* name : kFastRpcLibs) {
-        void* h = dlopen(name, RTLD_NOW | RTLD_LOCAL);
-        if (h == nullptr) {
-            IVLOGW("dlopen('%s') fallo: %s", name, dlerror());
+    // 1) Transporte FastRPC
+    for (const auto& c : kTransports) {
+        void* th = dlopen(c.lib, RTLD_NOW | RTLD_LOCAL);
+        if (th == nullptr) {
+            IVLOGW("dlopen('%s') fallo: %s", c.lib, dlerror());
             continue;
         }
-
-        DspVTable vt{};
-        vt.open             = resolve<fn_open_t>            (h, "ivanna_dsp_open");
-        vt.close            = resolve<fn_close_t>           (h, "ivanna_dsp_close");
-        vt.process_stereo   = resolve<fn_process_stereo_t>  (h, "ivanna_dsp_process_stereo");
-        vt.set_neuro_params = resolve<fn_set_neuro_params_t>(h, "ivanna_dsp_set_neuro_params");
-        vt.get_metrics      = resolve<fn_get_metrics_t>     (h, "ivanna_dsp_get_metrics");
-
-        // Contrato mínimo: open + close + al menos una operación útil.
-        const bool minimum_ok =
-            vt.open != nullptr &&
-            vt.close != nullptr &&
-            (vt.process_stereo != nullptr || vt.set_neuro_params != nullptr);
-
-        if (!minimum_ok) {
-            IVLOGW("libreria '%s' cargada pero sin simbolos IDL — descartando", name);
-            dlclose(h);
+        TransportVTable tv{};
+        tv.session_control = resolve<fn_remote_session_control_t>(th, "remote_session_control");
+        tv.rpcmem_init     = resolve<fn_rpcmem_init_t>            (th, "rpcmem_init");
+        tv.rpcmem_deinit   = resolve<fn_rpcmem_deinit_t>          (th, "rpcmem_deinit");
+        if (tv.session_control == nullptr) {
+            IVLOGW("'%s' cargada pero sin remote_session_control — descartando", c.lib);
+            dlclose(th);
             continue;
         }
-
-        g_lib_handle = h;
-        g_lib_loaded = name;
-        g_vt = vt;
+        // Fija dominio (cDSP/aDSP) — best-effort: si la ROM ignora el req, seguimos.
+        struct { uint32_t domain; uint32_t priority; } dom{ static_cast<uint32_t>(c.domain), 0 };
+        int sc = tv.session_control(DSPRPC_SET_DOMAIN, &dom, sizeof(dom));
+        if (sc != 0) {
+            IVLOGW("remote_session_control(SET_DOMAIN=%d) rc=%d (no fatal)", c.domain, sc);
+        }
+        if (tv.rpcmem_init) tv.rpcmem_init();
+
+        // 2) Stub IDL local
+        void* sh = dlopen(kStubLib, RTLD_NOW | RTLD_LOCAL);
+        if (sh == nullptr) {
+            IVLOGW("dlopen('%s') fallo: %s — Hexagon offload no disponible (falta stub qaic)",
+                   kStubLib, dlerror());
+            if (tv.rpcmem_deinit) tv.rpcmem_deinit();
+            dlclose(th);
+            continue;
+        }
+        DspVTable vt{};
+        vt.open             = resolve<fn_open_t>            (sh, "ivanna_dsp_open");
+        vt.close            = resolve<fn_close_t>           (sh, "ivanna_dsp_close");
+        vt.process_stereo   = resolve<fn_process_stereo_t>  (sh, "ivanna_dsp_process_stereo");
+        vt.set_neuro_params = resolve<fn_set_neuro_params_t>(sh, "ivanna_dsp_set_neuro_params");
+        vt.get_metrics      = resolve<fn_get_metrics_t>     (sh, "ivanna_dsp_get_metrics");
+        const bool minimum_ok = vt.open && vt.close &&
+            (vt.process_stereo != nullptr || vt.set_neuro_params != nullptr);
+        if (!minimum_ok) {
+            IVLOGW("stub '%s' sin simbolos IDL — descartando", kStubLib);
+            dlclose(sh);
+            if (tv.rpcmem_deinit) tv.rpcmem_deinit();
+            dlclose(th);
+            continue;
+        }
+
+        g_transport_handle = th;
+        g_transport_name   = c.lib;
+        g_active_domain    = c.domain;
+        g_stub_handle      = sh;
+        g_tv = tv;
+        g_vt = vt;
         g_dsp_available.store(true, std::memory_order_release);
-        IVLOGI("Hexagon DSP disponible via '%s' (open=%p process=%p)",
-               name,
-               reinterpret_cast<void*>(vt.open),
-               reinterpret_cast<void*>(vt.process_stereo));
-        return;
+        IVLOGI("Hexagon DSP activo: transporte=%s dominio=%d stub=%s",
+               c.lib, c.domain, kStubLib);
+        return true;
     }
     IVLOGI("Hexagon DSP no disponible — usando fallback CPU");
 #else
     IVLOGI("dlopen no soportado en esta plataforma — fallback CPU");
 #endif
     g_dsp_available.store(false, std::memory_order_release);
+    return false;
 }
 
 } // namespace
 
 // ── API pública consumida por fastrpc_client / npe_engine ────────────────────
 
 bool ensure_loaded() noexcept {
-    std::call_once(g_load_once, load_once);
-    return g_dsp_available.load(std::memory_order_acquire);
+    if (g_dsp_available.load(std::memory_order_acquire)) return true;
+    std::lock_guard<std::mutex> lk(g_load_mtx);
+    if (g_dsp_available.load(std::memory_order_acquire)) return true;
+    return try_load_locked();
 }
 
 bool is_available() noexcept {
     return g_dsp_available.load(std::memory_order_acquire);
 }
 
 const char* active_library() noexcept {
-    return g_lib_loaded ? g_lib_loaded : "";
+    return g_transport_name ? g_transport_name : "";
 }
 
+int active_domain() noexcept { return g_active_domain; }
+
 void release() noexcept {
 #if IVANNA_HAS_DLOPEN
-    if (g_lib_handle != nullptr) {
-        dlclose(g_lib_handle);
-        g_lib_handle = nullptr;
-        g_lib_loaded = nullptr;
+    std::lock_guard<std::mutex> lk(g_load_mtx);
+    if (g_stub_handle != nullptr) {
+        dlclose(g_stub_handle);
+        g_stub_handle = nullptr;
+    }
+    if (g_transport_handle != nullptr) {
+        if (g_tv.rpcmem_deinit) g_tv.rpcmem_deinit();
+        dlclose(g_transport_handle);
+        g_transport_handle = nullptr;
+        g_transport_name   = nullptr;
+        g_active_domain    = -1;
     }
 #endif
     g_vt = DspVTable{};
+    g_tv = TransportVTable{};
     g_dsp_available.store(false, std::memory_order_release);
 }
 
@@
 int dsp_get_metrics(void* handle, float* cpu_load, float* peak_amp) noexcept {
     if (!g_dsp_available.load(std::memory_order_acquire) ||
         g_vt.get_metrics == nullptr) return -1;
-    if (cpu_load) *cpu_load = 0.0f;
-    if (peak_amp) *peak_amp = 0.0f;
-    return g_vt.get_metrics(handle, cpu_load, peak_amp);
+    // No zeroar aquí: dejamos que el callee escriba los valores reales.
+    // Si falla, el llamador ya trata rc<0 como "sin métricas".
+    const int rc = g_vt.get_metrics(handle, cpu_load, peak_amp);
+    return rc;
 }
 
 }}} // namespace ivanna::hexagon::rt
diff --git a/app/src/main/cpp/hexagon/hexagon_dsp_integration.hpp b/app/src/main/cpp/hexagon/hexagon_dsp_integration.hpp
--- a/app/src/main/cpp/hexagon/hexagon_dsp_integration.hpp
+++ b/app/src/main/cpp/hexagon/hexagon_dsp_integration.hpp
@@
 bool is_available() noexcept;
 
 // Nombre de la librería nativa cargada, o cadena vacía si ninguna. Solo
 // para logging/telemetría — nunca dereferenciar como recurso.
 const char* active_library() noexcept;
 
+// Dominio FastRPC activo (0=aDSP, 3=cDSP, -1=ninguno). Solo telemetría.
+int active_domain() noexcept;
+
 // Libera el handle dlopen. Segura de llamar múltiples veces y sin carga previa.
 void release() noexcept;
 
 }} // namespace ivanna::hexagon
diff --git a/app/src/main/cpp/hexagon/hexagon_dsp_integration.cpp b/app/src/main/cpp/hexagon/hexagon_dsp_integration.cpp
--- a/app/src/main/cpp/hexagon/hexagon_dsp_integration.cpp
+++ b/app/src/main/cpp/hexagon/hexagon_dsp_integration.cpp
@@
 namespace rt {
     bool        ensure_loaded()   noexcept;
     bool        is_available()    noexcept;
     const char* active_library()  noexcept;
+    int         active_domain()   noexcept;
     void        release()         noexcept;
 }
 
 bool ensure_available() noexcept { return rt::ensure_loaded(); }
 bool is_available()     noexcept { return rt::is_available(); }
 const char* active_library() noexcept { return rt::active_library(); }
+int  active_domain()    noexcept { return rt::active_domain(); }
 void release() noexcept { rt::release(); }
 
 }} // namespace ivanna::hexagon
