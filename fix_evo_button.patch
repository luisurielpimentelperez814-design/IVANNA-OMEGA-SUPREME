diff --git a/src/components/EvolutionaryEqPanel.tsx b/src/components/EvolutionaryEqPanel.tsx
--- a/src/components/EvolutionaryEqPanel.tsx
+++ b/src/components/EvolutionaryEqPanel.tsx
@@ -127,7 +127,15 @@
           <span className="text-xs text-[#888]">256-Tap time-domain FIR · covariance adaptation · smooth phase penalization</span>
         </div>
         <button
-          onClick={() => setIsEvoRunning(!isEvoRunning)}
+          onClick={() => {
+            const nextState = !isEvoRunning;
+            setIsEvoRunning(nextState);
+            if (window.cefQuery) {
+              window.cefQuery({ request: JSON.stringify({ action: "toggle_cma_es", enabled: nextState }) });
+            } else {
+              console.log("CMA-ES Evolution state toggled:", nextState);
+            }
+          }}
           className={`px-4 py-2 rounded-lg text-xs font-bold transition-all border ${
             isEvoRunning
               ? 'bg-[#166534] border-[#4ADE80] text-[#BBF7D0] shadow-lg shadow-[#4ADE80]/20'
