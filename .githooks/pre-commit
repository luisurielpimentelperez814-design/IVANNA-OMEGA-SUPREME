#!/usr/bin/env bash
# Pre-commit: ejecutar chequeos locales
echo "Running pre-commit checks..."
if [ -f validation/scripts/run_local_checks.sh ]; then
  bash validation/scripts/run_local_checks.sh
fi
